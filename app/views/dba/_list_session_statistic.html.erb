<%
    @update_area = get_unique_area_id

    column_options =
    [
      {:caption=>"Name",   :data=>proc{|rec| rec.name}, :title=>"Name der Statistik"          },
      {:caption=>"Value",  :data=>proc{|rec| formattedNumber(rec.value) }, :align=>"right", :title=>"Statistik-Wert kumuliert seit Logon der Session"        },
    ]

%>
<%= gen_slickgrid(@stats, column_options,
                  {:caption=>"Session-Statistik aus gv$SessStat für Instance=#{@instance} , SID=#{@sid}, 0-Werte werden nicht angezeigt", :width=>:auto}
)%>

<div id="<%= @update_area%>"></div>
