# encoding: utf-8
# Wird von EnvHelper inkludiert, kann in App Engine überschrieben werden

module EnvExtensionHelper

  def check_credentials(current_database)
    # Check credentials and render statusmessage if returning false
    true
  end
end