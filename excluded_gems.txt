# This is a list of Gems that should be excluded from Panorama.jar
# In Gemfile they are with "gem name, require: false"
# In build.sh they are removed after bundle install
# benchmark
erb
io-console
irb
jar-dependencies
minitest
pp
prettyprint
prism
psych
reline

# rdoc 8.0.0 now depends on rbs which has a native extension
# rdoc < 8 does not have this dependency and is compatible with jRuby, so we use rdoc < 8.0
rdoc, 7.2.0
