# This is a list of Gems that should be excluded from Panorama.jar
# In Gemfile they are with "gem name, require: false"
# In build.sh they are removed after bundle install
# benchmark
erb
io-console

# Use the last version without prism dependency, because prism needs native extensions
# prism plans a separate gem for Ruby only. https://github.com/ruby/prism/issues/3959
irb, 1.16.0

jar-dependencies
mail
minitest
pp
prettyprint

# prism removed from list as long as irb is used without a prism dependency
#prism

psych
reline

# rdoc 8.0.0 now depends on rbs which has a native extension
# rdoc < 8 does not have this dependency and is compatible with jRuby, so we use rdoc < 8.0
rdoc, 7.2.0
